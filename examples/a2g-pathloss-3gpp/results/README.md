# Results — evidence convention

Kept runs land under `results/<name>_<date>/` with a `meta.json`. Gitignored except
`README.md`, `meta.json`, and curated summaries.

## `a2g-uma-av_2026-07-06/`

The adoption run: `python3 model.py` at the correct UMa-AV binding, plus `--check` passing.
A genuine, license-free run whose computed values match 3GPP TR 36.777 exactly.
